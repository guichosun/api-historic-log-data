server:
  port: 52661

spring:
  application:
    name: api-historic-log-data
  datasource:
    url: jdbc:h2:mem:usersDB # In memory DB.
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

# To show SQL sentences
logging:
  level:
    sql: debug